{
  "name": "jeu-de-dames",
  "version": "1.0.0",
  "description": "Jeu de dames classique avec IA et règles personnalisables",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build required'",
    "cap:init": "npx cap init 'Jeu de Dames' com.jeudedames.app --web-dir .",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "android:build": "cd android && ./gradlew assembleDebug",
    "android:build:release": "cd android && ./gradlew assembleRelease"
  },
  "keywords": [
    "jeu",
    "dames",
    "checkers",
    "game",
    "android"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "sharp": "^0.34.5"
  }
}
